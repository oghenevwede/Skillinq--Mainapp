declare module '*.css' {
  // You can keep it empty for just side-effect imports
  // or if you are using CSS Modules (which is unlikely for a 'globals.css')
  // you might define the expected structure, but for global CSS, this is enough.
}